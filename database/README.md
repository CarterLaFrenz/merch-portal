# Database

MySQL/MariaDB database for the Merch Portal.

## Setup

1. Install MySQL or MariaDB
2. Create database: `CREATE DATABASE merch_portal;`
3. Run schema: `mysql -u user -p merch_portal < schema.sql`

## Future

- Migration scripts will be added
- Seed data scripts will be added
- Connection pooling configuration
